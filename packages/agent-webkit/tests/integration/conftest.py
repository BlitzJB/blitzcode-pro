"""Integration-test fixtures.

Postgres fixtures (``pg_dsn``, ``fresh_pg_store``) live in the top-level
``tests/conftest.py`` so they're shared with ``tests/properties``.
"""
from __future__ import annotations
